AWSTemplateFormatVersion: '2010-09-09'
Description: >
  Core data-lake stack (no Glue jobs yet)
  - One S3 bucket for raw / staging / analytics / scripts / Athena temp
  - Glue IAM role for future jobs
  - Glue Database
  - Athena WorkGroup

Parameters:
  ProjectName:
    Type: String
    Default: caffine-analytics-storage
    Description: Prefix for the S3 bucket name
  GlueDatabaseName:
    Type: String
    Default: caffine_analytics_db
    Description: Glue database for analytics tables
  AthenaWorkGroupName:
    Type: String
    Default: caffine-analytics-workgroup
    Description: Athena WorkGroup writing results to the same bucket

Resources:
  DataBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub '${ProjectName}-${AWS::Region}-${AWS::AccountId}'
      VersioningConfiguration:
        Status: Enabled
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        IgnorePublicAcls: true
        BlockPublicPolicy: true
        RestrictPublicBuckets: true
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256

  DataBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref DataBucket
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Sid: DenyPublicAcl
            Effect: Deny
            Principal: "*"
            Action:
              - "s3:PutBucketAcl"
              - "s3:PutObjectAcl"
            Resource:
              - !Sub arn:aws:s3:::${DataBucket}
              - !Sub arn:aws:s3:::${DataBucket}/*

  GlueServiceRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub 'GlueETLRole-${AWS::StackName}'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - glue.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSGlueServiceRole
      Policies:
        - PolicyName: GlueS3AccessPolicy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Sid: S3Access
                Effect: Allow
                Action:
                  - s3:ListBucket
                  - s3:GetBucketLocation
                Resource:
                  - !Sub arn:aws:s3:::${DataBucket}
              - Sid: S3ObjectAccess
                Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:PutObject
                  - s3:DeleteObject
                Resource:
                  - !Sub arn:aws:s3:::${DataBucket}/*
                  - arn:aws:s3:::caff-dump/*
              - Sid: CloudWatchLogs
                Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource: "*"

  GlueDatabase:
    Type: AWS::Glue::Database
    Properties:
      CatalogId: !Ref AWS::AccountId
      DatabaseInput:
        Name: !Ref GlueDatabaseName
        Description: Glue database for analytics tables

  AthenaWorkGroup:
    Type: AWS::Athena::WorkGroup
    Properties:
      Name: !Ref AthenaWorkGroupName
      Description: WorkGroup writing results to the same bucket under /tmp/athena-results/
      WorkGroupConfiguration:
        EnforceWorkGroupConfiguration: true
        ResultConfiguration:
          OutputLocation: !Sub "s3://${DataBucket}/tmp/athena-results/"
        PublishCloudWatchMetricsEnabled: true

Outputs:
  DataBucket:
    Description: S3 bucket name for raw/staging/analytics/scripts/tmp
    Value: !Ref DataBucket
  GlueRoleArn:
    Description: IAM Role ARN for future Glue jobs
    Value: !GetAtt GlueServiceRole.Arn
  GlueDatabase:
    Description: Glue database name
    Value: !Ref GlueDatabaseName
  AthenaWorkGroup:
    Description: Athena workgroup configured to write results to s3://<bucket>/tmp/athena-results/
    Value: !Ref AthenaWorkGroupName
